{
  "name": "jazdy-doszkalajace-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "npm run migrate && node src/index.js",
    "dev": "npm run migrate && node --watch-path=src src/index.js",
    "migrate": "node src/db/migrate.js",
    "set-admin-password": "node scripts/set-admin-password.mjs"
  },
  "engines": {
    "node": ">=18.11.0"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^9.1.0"
  }
}
